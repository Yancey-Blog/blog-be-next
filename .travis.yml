language: node_js

sudo: true

branchs:
  only:
    - master

services:
  - mongodb

node_js:
  - '12'

cache:
  directories:
    - node_modules

install:
  - npm install

script:
  - npm run test
  - npm run build

after_success:
  - npm run codecov
